/*
 * Ex 2 — Fibonacci
 * Difficulty: ★★★★☆☆☆☆☆☆ (4/10)
 *
 * Return the Nth Fibonacci number.
 * fib(0)=0, fib(1)=1, fib(n)=fib(n-1)+fib(n-2)
 * Aim for O(n) — no naive recursion.
 */

/**
 * @param {number} n
 * @returns {number}
 */
const f = (n) => {
  // write your solution here
  return 0;
};

module.exports = f;
