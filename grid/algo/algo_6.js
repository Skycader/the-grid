/*
 * Ex 6 — Valid Brackets
 * Difficulty: ★★★★★☆☆☆☆☆ (5/10)
 *
 * Given a string containing only '(', ')', '{', '}', '[', ']',
 * determine if the input string is valid.
 * An input is valid if brackets are closed in the correct order.
 */

/**
 * @param {string} s
 * @returns {boolean}
 */
const f = (s) => {
  // write your solution here
  return false;
};

module.exports = f;
