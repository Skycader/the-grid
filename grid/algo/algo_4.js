/*
 * Ex 4 — Maximum Subarray (Kadane's Algorithm)
 * Difficulty: ★★★★★★☆☆☆☆ (6/10)
 *
 * Given an integer array, find the contiguous subarray
 * (containing at least one number) which has the largest sum
 * and return its sum.
 */

/**
 * @param {number[]} nums
 * @returns {number}
 */
const f = (nums) => {
  // write your solution here
  return 0;
};

module.exports = f;
