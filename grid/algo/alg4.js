/** Задача
 * Написать функцию add,
 * принимающую аргумент число,
 * до тех пор, пока не будет вызвана без аргументов.
 * Вернуть сумму всех аргументов.
 * add(9)(10)() => 19
 */

module.exports = f;
