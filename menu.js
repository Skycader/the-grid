// ═══════════════════════════════════════════════════════════════
//  GRID::RUNNER — menu.js
//  Category registry. Tasks are NOT listed here.
//  They are loaded automatically from:
//    config/{folder}.js
//  Each config file exports (via window assignment) an array
//  which is merged into the category at boot time.
// ═══════════════════════════════════════════════════════════════

// eslint-disable-next-line no-unused-vars
const MENU = [
  { id: "regex", name: "REGEX", desc: "Pattern Matching", folder: "regex" },
];
